def helper():
    info = """\
    --domain            Запуск скрипта с параметрами [domain] [type]
                        domain или email можно передать вторым параметром,
                        последним параметром передаем тип записи. Пример:
                        python3 --domain my@email.com MX
    """
    return info
